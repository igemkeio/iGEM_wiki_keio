# Notion 連携用の環境変数。コピーして web/.env.local を作り値を入れる。
# .env.local は Git にコミットしない（.gitignore 済み）。

# Notion Integration の Internal Integration Secret（ntn_ で始まる）
NOTION_TOKEN=

# 同期対象の Database ID（DB の URL の 32 文字の英数字）
NOTION_DATABASE_ID=
